import { CSSProperties } from "react";

export const Header = () => {
  return <div style={style}>Cat</div>;
};

const style: CSSProperties = {
  flex: 1,
  background: "#515760",
  color: "white",
  fontSize: 80,
};
